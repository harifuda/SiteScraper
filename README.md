# SiteScraper
- This is my simple web scraper done using Playwright for Java, which fetches and stores the first 100 items into a MySQL database running in a Docker container.
- The items are saved in a PriorityQueue and sorted from least to most expensive prior to storing in the database.
